# 🎯 Interview Questions – Blog Task

এটা আমার TypeScript assignment, যেটা আমাকে Programming Hero থেকে দেওয়া হয়েছে। এখানে আমাকে ৫টা প্রশ্ন থেকে যেকোনো ২টার উত্তর বাংলায় লিখতে বলা হয়েছে। আমি নিচের দুইটা প্রশ্ন বেছে নিয়েছি:

## ✅ বেছে নেওয়া প্রশ্নসমূহ

1. What is the use of the keyof keyword in TypeScript? Provide an example.
2. Provide an example of using union and intersection types in TypeScript.

---

## ✍️ ব্লগ (বাংলা)

### 1. `keyof` এর ব্যবহার

`keyof` অপারেটরটি একটি অবজেক্ট টাইপের (যেমন interface বা type) সমস্ত প্রোপার্টির নামগুলোর একটি ইউনিয়ন টাইপ তৈরি করে। সহজ কথায়, এটি একটি অবজেক্টের কী (key) বা প্রোপার্টিগুলোর নাম স্ট্রিং লিটারাল হিসেবে বের করে নিয়ে আসে। উক্ত অবজেক্ট কী এর বাইরে কিছু ইনপুট দিলে TypeScript ইরোর দেখাবে। যেমনঃ

```ts
type User = {
  name: string;
  age: number;
  isActive: boolean;
};

type UserKeys = keyof User;
// "name" | "age" | "isActive"

function getValue(obj: User, key: UserKeys) {
  return obj[key];
}
```
---

### 2. Union এবং Intersection Types এর উদাহরণ

**Union Type:**
ইউনিয়ন টাইপের মাধ্যমে একটি ভেরিয়েবলে একাধিক ধরনের মান রাখা যায়। দুটি বা তার বেশি টাইপকে `|` দিয়ে যুক্ত করা হয়। ভেরিয়েবলটি ওই যুক্ত টাইপগুলোর মধ্যে যেকোনো একটির মান গ্রহণ করতে পারে। যেমনঃ

```ts
type ID = number | string;

let userId: ID;
userId = 301; // OK
userId = "A401"; // OK
// userId = true; // Error
```

**Intersection Type:**
ইন্টারসেকশন টাইপের মাধ্যমে একাধিক টাইপকে একত্রিত করে একটি নতুন টাইপ তৈরি করা হয়, যেখানে নতুন টাইপটিতে সমস্ত টাইপের প্রোপার্টিগুলো থাকতে হবে। এটি `&` অপারেটর দিয়ে যুক্ত করা হয়। যেমনঃ

```ts
interface Student {
  name: string;
  age: number;
}

interface Captain {
  role: string;
}

type StudentCaptain = Student & Captain;

const student: StudentCaptain = {
  name: "Mizan",
  age: 18,
  role: "Captain",
};
```

---

## 🛠️ Tools ব্যবহার করা হয়েছে

- TypeScript
- Markdown

---

## 🎯 Task এর উদ্দেশ্য

এই কাজটি TypeScript-এর কিছু গুরুত্বপূর্ণ বেসিক বিষয় সহজভাবে ব্যাখ্যা করার জন্য দেওয়া হয়েছিল, যেগুলো চাকরির ইন্টারভিউয়ের জন্য অত্যন্ত গুরুত্বপূর্ণ। এই Taskটি আমার এই সংক্রান্ত কনসেপ্টগুলো পরিষ্কার করতে অনেক সাহায্য করেছে।
